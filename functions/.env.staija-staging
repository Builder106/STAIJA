# Per-project override for the APP_URL param declared in
# functions/src/emailTemplates.ts. Firebase CLI loads this file
# automatically when deploying to the staija-staging project, so
# transactional/newsletter email links point at staging instead of
# yanking recipients into prod Firestore.
APP_URL=https://staging.staija.org
